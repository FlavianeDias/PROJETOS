from kivymd.app import MDApp #chamando o kivy
from kivymd.uix.boxlayout import  MDBoxLayout #importando layout de botoes
from kivymd.uix.button import MDFlatButton
class TesteApp(MDApp): #criando uma classe
    def build(self):
        boxLayout = MDBoxLayout(
        )
        return

TesteApp().run() #rodando a tela